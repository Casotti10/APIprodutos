# Copy to .env and set your backend base url (include the /api suffix)
# Example for a remote backend named "plutal":
# VITE_API_BASE=https://plutal.example.com/api

# Local dev default uses http://localhost:8080/api when VITE_API_BASE is not set
